{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main CodeIt window to receive local streamed chat events.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
