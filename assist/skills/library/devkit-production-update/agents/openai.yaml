interface:
  display_name: "DevKit Production Update"
  short_description: "Safely verify and deploy DevKit on its Ubuntu VPS."
  default_prompt: "Use $devkit-production-update to verify, deploy, migrate, clean, and audit a DevKit production release."
